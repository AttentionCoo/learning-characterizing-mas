<div align="center">

# LearnAgent：基于大模型的个性化资源生成与学习多智能体系统

网址: https://8.141.124.251

**第十五届"中国软件杯"大学生软件设计大赛 · A3 赛题参赛作品**

赛题名称：基于大模型的个性化资源生成与学习多智能体系统开发 ｜ 出题企业：科大讯飞股份有限公司

</div>

---

## 一、项目定位与总体简介

本项目是一个面向**高等教育个性化学习**场景（以脑卒中医学教育为示范领域）的智能多智能体系统，旨在通过大语言模型与多智能体协同技术，实现"因材施教"的教育理念工程化落地。系统深度融合**检索增强生成（RAG）、多智能体协同推理、个性化学习画像、多模态影像识别与 PubMed 国际文献检索**，构建了从学习画像构建、个性化资源生成、智能辅导问答、学习路径规划到学习效果评估的完整闭环。

与传统通用问答系统不同，本项目以权威教材与学术文献为知识底座，通过"**意图智能分流—多智能体协同推理—自愈式反思校验—个性化报告生成**"的结构化流程，使每一次输出都具备明确证据来源与逻辑依据。系统强调"**证据先行、过程可解释、结果可验证、质量可管控**"，创新性引入 PubMed 国际文献检索作为本地知识库的循证扩展，实现"权威指南 + 前沿文献"的双重证据保障。

### 目标用户

| 用户 | 核心价值 |
|:---|:---|
| **高校学生** | 通过自然语言对话自动构建个性化学习画像，获得量身定制的学习资源、辅导答疑与学习路径规划，实现"千人千面"学习体验 |
| **高校教师** | 快速生成适配不同学生水平的教学资源，大幅减轻备课负担，提升教学效率 |
| **医学教育场景** | 以脑卒中医学教育为示范领域，提供从理论学习到临床思维训练的全链路支持，多模态联合推理支持医学影像分析，PubMed 提供前沿循证医学证据，代码辅助支持医学数据分析编程 |

### 核心量化指标

| 指标 | 实测数据 |
|:---|:---|
| 自动化测试用例 | **71 条**（黑盒 33 + 白盒 38），通过率 100% |
| 白盒路径覆盖率 | **100%**（核心模块路径覆盖 + 共享记忆系统 + RAG + 迁移验证，38 条路径全部覆盖） |
| 10 并发 SSE 成功率 | **100%**，P95 延迟 18.3s |
| 50 并发 SSE 成功率 | **98%**，P95 延迟 28.6s |
| 100 并发 SSE 成功率 | **94%**，P95 延迟 42.7s |
| 非 AI 接口 P95 延迟 | **< 60ms**（全部接口） |
| 画像维度覆盖率 | **100%**（8/8 维度全覆盖） |
| 退火修正成功率 | **92%**（23/25 次校验驳回后修正通过） |
| RAG 混合检索召回率提升 | **+15%**（相比纯向量检索） |
| 资源推送关联率 | **92%**（每步平均 1.8 个精准资源） |

---

## 二、系统整体功能简介

系统围绕真实学习场景，构建了八大核心功能模块，形成"**画像—资源—辅导—路径—评估—多模态—循证—代码**"的完整个性化学习闭环。

### （一）对话式学习画像自主构建

系统个性化基础模块，摒弃传统繁琐表单，通过自然语言对话自动构建动态学生画像。

- **对话式特征抽取**：画像对话智能体与特征抽取智能体协同工作，自动从对话中抽取结构化特征
- **8 维度动态画像**：知识基础、认知风格、学习目标、易错模式、学习节奏、资源偏好、临床经验、情绪状态，每个维度包含枚举化等级和结构化描述，全面覆盖赛题要求的不少于 6 个维度
- **画像随学随新**：对话结束后自动提取画像维度，支持多轮对话上下文摘要，确保长对话场景下的上下文连贯性
- **手动编辑与 AI 生成结合**：支持用户对 AI 生成的画像维度进行手动编辑修正

### （二）多智能体协同的个性化资源生成

系统核心功能模块，体现"多智能体"架构设计，通过多个智能体协同生成个性化学习资源。

- **7 类学习资源生成**：课程讲解文档、知识体系思维导图、练习题目、临床指南与文献、教学视频脚本、医学编程实操、代码实操案例，每种资源均有独立 API 端点和专用请求模型
- **8 智能体协同生成**：画像对话、特征抽取、需求分析、文档撰写、题目生成、质量审核、学习激励、仲裁智能体协同工作，通过 YAML 配置驱动
- **辩论-仲裁模式**：不同智能体就争议知识点提出对立观点，经多轮辩论后由仲裁智能体依据证据链裁决，减少单一模型偏见
- **学习激励与情感陪伴**：学习激励智能体根据学生情绪状态提供鼓励与节奏调整建议，嵌入最终报告
- **难度自适应**：根据学生画像自动调整资源难度级别（入门/中级/进阶）
- **质量审核闭环**：质量审核智能体进行医学准确性、个性化匹配度和教学有效性三重校验
- **动态退火策略**：校验失败后自动分类驳回原因（5 类），生成针对性修正提示词，衰减不通过智能体的发言权重
- **智能体动态编排**：根据意图类型和难度评分动态裁剪参与智能体数量，简单任务 3 个专家，复杂任务 6 个 + 仲裁

### （三）智能辅导问答

为学习者提供一对一的智能辅导服务，支持多轮对话与上下文关联。

- **多轮对话辅导**：支持连续追问，自动关联历史对话上下文，支持传入课程名、知识点、学习路径 ID 等上下文
- **流式思考过程展示**：实时展示 AI 推理思考步骤，让学生不仅获得答案，更能理解推理过程
- **多模态辅导支持**：支持上传医学影像、检验报告、课件截图等，自动检测图片类型并匹配分析策略
- **偏好回答形式**：可指定期望的回答形式（text/diagram/code/video）

### （四）个性化学习路径规划

基于学生画像自动规划分阶段的学习路径，支持动态调整与进度追踪。

- **分阶段路径规划**：自动生成 5-15 步学习路径，每步包含 2-5 个知识点和前置步骤依赖，难度循序渐进
- **精准资源推送**：基于画像维度综合计算（0.3×关键词匹配 + 0.3×难度适配 + 0.2×偏好匹配 + 0.2×薄弱项加权），推送匹配的学习资源，返回推荐理由和相关性评分
- **进度追踪与状态管理**：支持标记完成状态、记录投入时长与自评反馈
- **动态路径调整**：支持插入步骤、更新资源、调整难度三种调整类型

### （五）学习效果评估

对学习效果进行多维度量化评估，形成"学习—评估—调整"的闭环。

- **5 维度量化评估**：知识掌握度、学习效率、技能应用、学习一致性、进度对齐度
- **知识点掌握热力图**：按知识点树状结构展示掌握程度（0.0-1.0 四档）
- **练习/测验提交与批改**：支持答案提交与自动批改，返回知识点薄弱分析和推荐资源
- **评估驱动迭代**：评估结果自动触发学习路径调整和画像更新，形成完整闭环

### （六）多模态影像识别与辅助分析

集成 qwen-vl-max 多模态视觉大模型，支持图片上传实现多模态辅助分析。

- **图片类型自动识别**：自动判断图片类型（课件笔记/代码编程/通用医学影像），匹配差异化分析策略
- **流式影像分析**：流式输出分析结果，无缝融入主对话流式管道
- **多图联合分析**：支持单次请求上传多张图片进行联合分析

### （七）PubMed 国际文献检索与循证扩展

集成 PubMed E-utilities API，为学习资源生成提供循证医学证据扩展。

- **智能检索**：esearch + efetch 两步检索，支持关键词检索与摘要获取
- **8 级证据等级排序**：Practice Guideline > Guideline > Meta-Analysis > Systematic Review > RCT > Clinical Trial > Review > Case Reports
- **与本地 RAG 互补**：本地知识库提供权威指南精准溯源，PubMed 提供最新国际研究进展，实现双重证据保障
- **异常容错**：多层异常处理，检索失败时优雅降级，不影响主流程

### （八）代码辅助开发

面向医学生的代码辅助开发能力，支持医学数据分析编程与临床决策支持代码生成。

- **代码实操案例生成**：作为 7 类学习资源之一，共享多智能体协同推理管道
- **代码执行与调试**：提供代码执行沙箱接口，支持代码运行与调试
- **代码辅助**：提供代码补全、错误诊断与优化建议

---

## 三、系统架构与前后端设计

本项目采用前后端分离与模型服务独立部署的三层架构，将用户交互、业务逻辑与 AI 推理能力深度解耦。

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         前端交互层 (Vue 3.5 / SSE)                      │
│  ┌────────────────┐  ┌──────────────────┐  ┌─────────────────────────┐ │
│  │ 学习画像对话面板 │  │ 资源生成与预览    │  │ 思考过程与报告流式渲染  │ │
│  └────────────────┘  └──────────────────┘  └─────────────────────────┘ │
│  ┌────────────────┐  ┌──────────────────┐  ┌─────────────────────────┐ │
│  │ 多模态图片上传  │  │ 代码辅助与执行    │  │ PubMed 文献检索结果展示 │ │
│  └────────────────┘  └──────────────────┘  └─────────────────────────┘ │
├─────────────────────────┬────────────────────────────┬─────────────────┤
│        RESTful API      │                            │   SSE 流式响应   │
├─────────────────────────▼────────────────────────────▼─────────────────┤
│                  后端服务层 (Spring Boot 3.3 / WebFlux)                 │
│  ┌────────────────┐  ┌──────────────────┐  ┌─────────────────────────┐ │
│  │ 安全认证 (JWT)  │  │ 限流与并发控制    │  │ WebClient 响应式转发引擎│ │
│  └────────────────┘  └──────────────────┘  └─────────────────────────┘ │
│  ┌────────────────┐  ┌──────────────────┐  ┌─────────────────────────┐ │
│  │ 代码执行沙箱    │  │ OSS 文档管理      │  │ SSE 事件缓存与断线续传  │ │
│  └────────────────┘  └──────────────────┘  └─────────────────────────┘ │
├─────────────────────────┬────────────────────────────┬─────────────────┤
│                         │      Async Generator       │                 │
├─────────────────────────▼────────────────────────────▼─────────────────┤
│                  模型推理服务层 (FastAPI / Python)                       │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ 三阶漏斗混合检索：向量+BM25宽召回 → RRF粗排 → Reranker精排(4模型容灾)│ │
│  ├───────────────────────────────────────────────────────────────────┤ │
│  │ 多模态视觉网关 (Qwen-VL-Max)：解析医学影像与检验报告              │ │
│  ├───────────────────────────────────────────────────────────────────┤ │
│  │ PubMed 文献检索 (E-utilities)：8级证据等级排序 + 国际循证扩展     │ │
│  ├───────────────────────────────────────────────────────────────────┤ │
│  │ 八智能体协同推理网络 (LangGraph)：意图分流→分析→检索→推理→校验→报告│ │
│  └───────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
```

### （一）前端交互层（Vue 3.5）

| 能力 | 实现方式 |
|:---|:---|
| 核心功能 | 学习画像对话、资源生成与预览、智能辅导、学习路径与效果评估的统一展示，支持模块快速切换、PDF 在线预览、图片拖拽上传与自动压缩 |
| 6 个核心页面 | 登录页、学习画像页、资源生成页、学习路径页、智能辅导页、学习评估页，均需登录后访问 |
| 流式渲染 | 基于 ReadableStream 的流式数据接收与实时打字机渲染，ThinkingPanel 展示 AI 推理思考步骤 |
| Markdown 安全渲染 | marked 17 + DOMPurify 3.3，支持表格、列表、公式等富文本，XSS 防护 |
| PDF 在线预览 | pdfjs-dist 3.11 + vue-pdf-embed 2.1，支持指南文献在线阅读 |

### （二）后端服务层（Spring Boot 3.3 / WebFlux）

| 能力 | 实现方式 |
|:---|:---|
| 安全与控制 | JWT 身份认证与权限隔离，Token 自动续期，Redisson 分布式限流与并发信号量控制（permits=20） |
| 调度与中转 | 响应式编程模型，WebClient 非阻塞流式调用，端到端高效数据链路 |
| SSE 断线续传 | 滑动窗口缓存（TTL=5min）+ Last-Event-ID 机制，保障弱网环境下的用户体验 |
| 对话持久化 | 对话历史持久化至 MySQL（talk + cont 双表），支持多轮对话上下文恢复，持久化失败自动重试（最多 3 次） |
| 代码执行沙箱 | 代码执行与辅助接口，支持运行调试与补全建议 |
| OSS 文档管理 | 阿里云 OSS 集成，支持文档上传、存储与在线预览 |

### （三）模型推理服务层（FastAPI / Python）

系统智能能力的核心大脑，服务启动时通过 lifespan 执行 7 步资源初始化。

| 能力 | 实现方式 |
|:---|:---|
| 统一入口加载 | Qwen-Max/Plus/Turbo 三级大模型、qwen-vl-max 多模态模型、PubMed 文献检索、三阶漏斗混合检索引擎（向量+BM25→RRF粗排→Reranker精排，4 模型容灾切换）及 LangGraph 状态机推理模块 |
| 意图智能分流 | 7 类意图分类 + 难度评分，通用知识问答走极速通道，个性化需求走多智能体协同推理通道 |
| 异步流式输出 | 将 LangGraph 原始事件翻译为标准事件格式，AsyncTaskManager 管理 SSE 流式任务生命周期 |
| JWT 双向认证 | 模型服务与后端之间通过 JWT 共享密钥实现双向认证 |

### （四）三层协同数据流

```
 用户输入/图片上传
       │
       ▼
 前端发送请求
       │
       ▼
 后端 JWT 鉴权与 Redisson 限流
       │
       ▼
 WebClient 异步非阻塞调用
       │
       ▼
 FastAPI 接收请求 ──► AsyncTaskManager 创建后台任务
                           │
                           ▼
                    LearningAgent 推理
                           │
                           ▼
              LangGraph StateGraph astream_events
                           │
                           ▼
              _translate_event 翻译标准事件
                           │
              ┌────────────┴────────────┐
              ▼                         ▼
     Java Flux 持续转发        Vue3 ReadableStream 实时渲染
```

---

## 四、核心技术与模型能力

### （一）检索增强生成（RAG）机制

系统以教材与学术文献作为知识底座，实施证据获取前置，采用**三阶漏斗混合检索架构**。

| 技术点 | 说明 |
|:---|:---|
| **三阶漏斗检索** | 宽召回（向量 + BM25 各 top-20）→ RRF 粗排（40→20 篇）→ Reranker 精排（20→3 篇），兼顾语义相似性与专业术语精确匹配 |
| **向量检索** | ChromaDB + DashScope Embedding (text-embedding-v2)，语义级相似度检索 |
| **BM25 关键词检索** | 基于 LangChain BM25Retriever，弥补向量检索对专业术语精确匹配的不足 |
| **RRF 倒数排名融合** | 零成本零延迟，纯排名融合避开 Dense/Sparse 分值区间差异，公式：RRF(d) = 1/(60+Rankᵥ) + 1/(60+Rank_b) |
| **多模型 Rerank 容灾** | 4 个候选重排模型自动切换（qwen-rerank-v1 → gte-rerank-v2 → qwen-rerank → gte-rerank），遇 AccessDenied 自动降级，全部失败时原始结果兜底 |
| **QA 自动衍生引擎** | 自动从 PDF 文档中衍生问答对，每 10 个 chunk 合并批量生成 3-5 个 QA 对，附带原文页码标签与来源信息，提升检索召回率 |
| **文档切分** | RecursiveCharacterTextSplitter，chunk_size=512, overlap=128 |
| **检索缓存** | 内置 MD5 键 + 300 秒 TTL 的内存缓存，避免重复检索 |
| **精准溯源标注** | 对采纳的证据强制进行来源标注（文献名称 + 页码），使输出具备可溯源验证能力 |

#### Hybrid RAG 三阶漏斗核心算法伪代码

```
算法: HybridRAG(query) → top_k_docs

1. 第一阶 · 宽召回 (Wide Recall):
   v_docs ← ChromaDB.similarity_search(query_embedding, k=20)
   b_docs ← BM25Retriever.invoke(query, k=20)
   日志: "向量检索 {len(v_docs)} 条 + BM25检索 {len(b_docs)} 条"

2. 第二阶 · RRF 粗排 (Coarse Ranking):
   FOR each doc_id IN (v_docs ∪ b_docs):
     rrf_score = 1/(60 + rank_dense[doc_id]) + 1/(60 + rank_sparse[doc_id])
   coarse_candidates ← sorted(rrf_score, descending)[:20]
   日志: "{len(v_docs)+len(b_docs)} 篇 → {len(coarse_candidates)} 篇候选"

3. 第三阶 · Reranker 精排 (Fine Ranking):
   FOR model IN [qwen-rerank-v1, gte-rerank-v2, qwen-rerank, gte-rerank]:
     TRY: result ← DashScope.TextReRank(model, query, coarse_candidates, top_n=3)
          IF result.status == OK: RETURN result
     CATCH AccessDenied / Exception: CONTINUE
   RETURN coarse_candidates[:3]  // 全部失败 → 原始结果兜底

4. 缓存写入:
   cache[MD5(query+top_k)] ← (result, timestamp)
   TTL = 300s
```

### （二）PubMed 国际文献检索与循证扩展

在本地知识库检索基础上，引入 PubMed 国际文献检索作为循证扩展。

| 技术点 | 说明 |
|:---|:---|
| **esearch + efetch 两步检索** | esearch 按相关性排序返回 PMID 列表，efetch 批量获取文献详细信息 |
| **8 级证据等级排序** | 自动按证据强度排序检索结果，确保高等级证据优先呈现 |
| **API Key 加速** | 配置 PubMed API Key 提升请求速率限制（3 次/秒 → 10 次/秒） |
| **异常容错** | HTTP 状态码异常、请求超时、XML 解析失败等多层异常处理，检索失败时优雅降级 |
| **与本地 RAG 互补** | 本地知识库提供权威指南精准溯源，PubMed 提供最新国际研究进展，双重证据覆盖 |

### （三）证据重排与上下文优化

- **gte-rerank 精准重排**：对检索结果进行语义相关性精排，确保喂给推理节点的证据质量
- **上下文压缩**：限制证据长度并进行结构化聚合，避免冗余信息干扰模型推理效率
- **精准溯源标注**：强制进行来源标注（文献名称、章节页码），使输出具备可溯源验证能力

### （四）多模态联合推理

- **医学影像理解**：通过 qwen-vl-max 对上传的急诊头颅 CT 影像进行视觉语义理解
- **检验报告解析**：自动读取并解析报告单图片，将异常指标转化为结构化文本
- **图片类型自动检测**：根据用户问题关键词自动检测图片类型，选择对应分析策略
- **图片压缩上传**：前端支持图片拖拽上传并自动压缩，保障传输效率

### （五）八智能体协同推理机制

系统基于 LangGraph StateGraph 设计了八智能体协同推理架构，融合辩论-仲裁模式与动态退火策略，模拟真实教学团队的"集体备课、辩论裁决与质量把关"流程。

```
                        【 LangGraph 状态机推理拓扑 】

  用户输入 ──► IntentNode (意图分类 + 难度评分)
                    │
                    ├── irrelevant ──────────► 拒绝
                    ├── knowledge ───────────► KnowledgeNode (极速知识问答)
                    └── profile / resource / tutor / assessment / learning_path
                                │
                                ▼
                        AnalysisNode (需求分析)
                                │
                                ▼
                        RetrieveNode (证据检索)
                                │
                                ▼
                 ┌──── ReasonNode ────────────────────┐
                 │  动态编排 → 八智能体并行推理         │
                 │  辩论-仲裁 → 证据链裁决             │
                 │  意见综合 → Proposal + Critique      │
                 └─────────────┬──────────────────────┘
                               │
                               ▼
                     ValidateNode (质量校验)
                      │              │
                  PASS ↓          FAIL ↓ (退火修正)
                      │              │
                      │         反思次数 < 3 → 返回 ReasonNode
                      │         反思次数 ≥ 3 → 强制输出
                      ▼
                     ReportNode (报告生成 + 学习激励)
```

#### 智能体角色矩阵

| 角色 | 优先级 | 适用意图 | 职责 |
|:---|:---:|:---|:---|
| 画像对话智能体 | 1 | 全场景 | 引导式对话，收集学生信息 |
| 特征抽取智能体 | 2 | profile / assessment | 从对话中抽取结构化特征（8 维度画像） |
| 需求分析智能体 | 3 | resource / tutor / learning_path | 结合画像分析学习需求，拆解生成任务 |
| 文档撰写智能体 | 4 | resource | 生成课程讲解文档、临床案例等教学内容 |
| 题目生成智能体 | 5 | resource / assessment | 生成多种类型练习题目 |
| 质量审核智能体 | 6 | resource / assessment / learning_path | 审核资源质量（准确性、个性化匹配度、教学有效性） |
| 学习激励智能体 | 7 | 全场景 | 根据情绪状态提供鼓励与学习节奏调整建议 |
| 仲裁智能体 | 8 | resource / tutor / assessment / learning_path | 依据证据链裁决辩论中的对立观点 |

#### 动态编排规则

| 意图类型 | 参与专家 | 数量 |
|:---|:---|:---:|
| profile | 画像对话 + 特征抽取 + 学习激励 | 3 |
| resource | 画像对话 + 需求分析 + 文档撰写 + 题目生成 + 质量审核 + 学习激励 | 6 |
| tutor | 画像对话 + 需求分析 + 质量审核 + 学习激励 | 4 |
| assessment | 特征抽取 + 需求分析 + 题目生成 + 质量审核 + 学习激励 | 5 |
| learning_path | 画像对话 + 需求分析 + 质量审核 + 学习激励 | 4 |
| knowledge | 直接 LLM 知识问答（不走完整工作流） | 1 |

> 仲裁智能体在辩论启用且难度评分 ≥ 0.6 时自动加入。

#### 多智能体推理核心算法伪代码

```
算法: MultiAgentReasoning(state: LearningState) → Dict

1. 动态编排 ← _resolve_active_experts(intent_type, difficulty)
   根据 intent_type 和 difficulty 从专家池筛选活跃专家
   若 difficulty ≥ 0.6 且辩论开启 → 自动加入仲裁智能体

2. 构建案例信息 ← _build_case_info(state)
   若 intent ≠ profile → 注入画像摘要供个性化适配
   若 validation_feedback 非空 → 追加退火修正指引

3. 并行推理 ← asyncio.gather(*[_ask_expert(role, instruction, case_info, weight)
                                 for role in active_experts])
   每个专家独立调用 LLM，权重 < 1.0 时追加"谨慎发言"提示

4. IF 辩论开启 AND 活跃专家数 > 1:
     FOR round = 1 TO debate_max_rounds:
       并行辩论 ← asyncio.gather(*[_ask_debater(role, debate_context, round)
                                     for role in debate_roles])
       更新辩论上下文 (含历史记录最近6条)
     仲裁裁决 ← _run_arbitration(debate_history, evidence)

5. 意见综合 ← LLM_synthesis.invoke(
     mode_directive(intent_type) + 加权专家意见文本 + 仲裁裁决(若有))
   输出按 "### PROPOSAL ###" / "### CRITIQUE ###" 分割

6. RETURN {proposal, critique, active_experts, debate_history, agent_weights}
```

#### 协同推理流程

```
 ┌─────────────────────────────────────────────────────────────────────┐
 │  Step 1 · 动态编排                                                   │
 │  根据意图与难度评分动态选择参与专家，简单任务 3 位，复杂任务全量协同     │
 ├─────────────────────────────────────────────────────────────────────┤
 │  Step 2 · 并行推理                                                   │
 │  选中的智能体并行调用 LLM 各自独立产出专业建议                         │
 │  学习激励智能体同步输出情感陪伴建议                                    │
 ├─────────────────────────────────────────────────────────────────────┤
 │  Step 3 · 辩论-仲裁                                                  │
 │  不同智能体就分歧观点进行多轮辩论                                      │
 │  仲裁智能体依据证据链与逻辑一致性做出最终裁决                           │
 ├─────────────────────────────────────────────────────────────────────┤
 │  Step 4 · 统筹汇总                                                   │
 │  将各智能体建议（带权重标签）与仲裁裁决合并                             │
 │  整合为综合提案（Proposal）与潜在问题批评（Critique）                   │
 ├─────────────────────────────────────────────────────────────────────┤
 │  Step 5 · 质量校验                                                   │
 │  双层校验——规则引擎检查 + LLM 反思检查                                │
 ├─────────────────────────────────────────────────────────────────────┤
 │  Step 6 · 动态退火反思循环                                            │
 │  校验未通过时自动分类驳回原因（5 类）                                   │
 │  生成针对性修正提示词，衰减发言权重，避免无效重试                        │
 └─────────────────────────────────────────────────────────────────────┘
```

### （六）动态退火校验算法

质量校验节点实现"驳回→分类→衰减→修正"的自愈式反思循环，是系统防幻觉的核心保障：

```
算法: ValidateWithAnnealing(state) → Dict

1. 规则引擎检查:
   FOR each (category, rules) IN contraindication_rules:
     IF category ∈ proposal AND ∃ rule ∈ rules WHERE rule ∈ proposal:
       RETURN _fail("触发[{category}]质量规则拦截: {rule}")

2. LLM 反思检查:
   verdict ← LLM.invoke("校验 proposal 是否存在严重错误...")
   IF verdict.startswith("REJECT"):
     reason ← 提取驳回理由 → GOTO 步骤3
   ELSE:
     RETURN {validation_passed: True}

3. 驳回原因分类:
   "事实" → factual_error, "逻辑" → logical_contradiction,
   "个性化" → personalization_insufficient,
   "专业" → professional_error, "不完整" → incomplete_content

4. 动态退火策略:
   correction_prompt ← get_correction_prompt(category)
   FOR each role IN active_experts:
     IF agent_weights[role] > 0.2:
       agent_weights[role] *= 0.7  // 权重衰减，最低 0.2

5. 反思次数检查:
   reflection_count += 1
   IF reflection_count < 3: → 回到推理节点
   ELSE: → 强制输出
```

**退火校验实测数据**：

| 指标 | 实测数据 |
|:---|:---|
| 校验驳回触发率 | 32%（8/25 次高难度资源生成触发驳回） |
| 驳回后修正成功率 | 92%（23/25 次修正后通过校验） |
| 平均反思轮次 | 1.4 轮（最大 3 轮） |
| 强制输出率 | 8%（2/25 次达反思上限后强制输出） |

### （七）流式推理与自动评测机制

- **Token 聚合机制**：通过 SSE 协议逐步传递思考过程和模型文本，对高频输出进行合并处理，减少前端渲染压力
- **事件翻译机制**：将 LangGraph 原始事件翻译为前端可理解的标准事件格式（node_start/token/node_done/done）
- **SSE 断线续传**：滑动窗口缓存（TTL=5min）+ Last-Event-ID 机制，断线重连后自动回放缓存事件
- **基于 RAGAS 框架的自动化评测**：从回答相关性、事实一致性、上下文精准度等维度对系统结果进行量化追踪

---

## 五、数据库设计

系统采用 MySQL 8.0 关系型数据库，共设计 **14 张业务表**，覆盖用户管理、对话持久化、画像存储、资源管理、学习路径、行为记录、效果评估等完整业务域。

### ER 关系图

```
┌──────────┐       ┌──────────────┐       ┌──────────┐
│   user   │──1:1──│student_profile│       │   talk   │
│          │       └──────────────┘       │          │
│          │──1:N────────────────────────►│          │
│          │                              └────┬─────┘
│          │                                   │1:N
│          │                              ┌────▼─────┐
│          │                              │   cont   │
│          │                              └──────────┘
│          │──1:N──┌────────────────┐
│          │       │learning_resource│
│          │       └────────────────┘
│          │──1:N──┌──────────────┐       ┌──────────────────┐
│          │       │ learning_path │──1:N──│learning_path_step│
│          │       └──────────────┘       └────────┬─────────┘
│          │──1:N──┌────────────────┐               │M:N
│          │       │learning_behavior│       ┌──────▼──────┐
│          │       └────────────────┘       │step_resource │
│          │──1:N──┌─────────────────┐      └──────┬──────┘
│          │       │evaluation_report │             │
│          │       └─────────────────┘      ┌──────▼──────┐
│          │                                │learning_    │
│          │                                │resource     │
└──────────┘                                └─────────────┘
```

### 核心表设计要点

| 表名 | 核心字段 | 设计要点 |
|:---|:---|:---|
| student_profile | dimensions(JSON), version | 8 维度画像以 JSON 存储，支持版本化自增 |
| talk / cont | content, role, images | 对话持久化双表，cont 支持 SSE 事件级粒度 |
| learning_path_step | knowledge_points(JSON), prerequisites(JSON), difficulty | 步骤级知识点与前置依赖以 JSON 存储 |
| step_resource | relevance(DECIMAL), is_recommended | 资源推送关联表，含相关性评分与推荐标记 |
| learning_behavior | behavior_type, duration, score, detail(JSON) | 行为类型枚举 + JSON 扩展字段 |
| evaluation_report | dimensions(JSON), strengths(JSON), suggestions(JSON) | 5 维度评估 + 优势/薄弱/建议 JSON 数组 |

---

## 六、安全与质量保障体系

### （一）系统安全机制

| 机制 | 实现方式 | 验证结果 |
|:---|:---|:---|
| JWT 双向认证 | Java 与 Python 共享 JWT Secret，双向验证 | ✅ 伪造 Token 被拒绝，过期 Token 自动续期 |
| 分布式限流 | Redisson 信号量（permits=20）+ 滑动窗口 | ✅ 超限请求返回 429，100 并发下成功率 94% |
| SQL 注入防护 | MyBatis-Plus 参数化查询 + 输入校验 | ✅ 注入攻击全部拦截 |
| XSS 防护 | DOMPurify 前端净化 + CSP 响应头 | ✅ 恶意脚本全部过滤 |
| 文件上传安全 | 白名单扩展名 + 大小限制 + OSS 隔离 | ✅ 非法文件全部拒绝 |
| 越权访问防护 | JWT Token 绑定用户 ID + 接口级权限校验 | ✅ 跨用户访问全部拒绝 |

### （二）容灾策略

| 场景 | 策略 | 降级方案 | 验证结果 |
|:---|:---|:---|:---|
| Rerank 模型不可用 | 4 模型自动切换 | 原始检索结果兜底 | ✅ 主模型失败后 0.8s 切换备选 |
| PubMed API 超时 | 超时熔断 | 仅使用本地 ChromaDB | ✅ 超时后 200ms 内降级 |
| OSS 上传失败 | 重试 + 降级 | 本地临时存储 | ✅ 重试 2 次后降级成功 |
| SSE 连接中断 | Last-Event-ID | 缓存事件回放 | ✅ 断线 5min 内重连可续传 |

### （三）测试体系

| 测试类型 | 用例数 | 覆盖范围 | 结果 |
|:---|:---:|:---|:---|
| 黑盒功能测试 | 33 | 10 大功能模块（认证/画像/资源/辅导/路径/评估/代码/对话/并发/非AI） | ✅ 全部通过 |
| 白盒路径覆盖 | 38 | 核心模块路径覆盖 + 共享记忆系统 + RAG + 迁移验证 | ✅ 路径覆盖率 100% |
| 并发性能测试 | 3 级梯度 | 10/50/100 并发 SSE | ✅ 100 并发成功率 94% |
| 安全测试 | 16 | JWT / 注入 / 限流 / 越权 / 上传 | ✅ 全部通过 |
| 容灾测试 | 7 | Rerank / PubMed / OSS / SSE 降级 | ✅ 全部通过 |

---

## 七、创新点与作品亮点

### 创新点一：证据驱动的教育推理范式创新

打破传统大模型"先生成再解释"导致的知识幻觉，将证据获取前置。模型在生成答案前必须依赖外部专业知识库的检索证据，采用三阶漏斗混合检索（向量+BM25宽召回 → RRF粗排 → Reranker精排），实现了从"模型主导"向"证据主导"的范式跨越，显著提升教育内容的准确性与可信度。实测数据显示，RAG 混合检索相比纯向量检索召回率提升 **15%**。

### 创新点二：八智能体协同的辩论-仲裁质量管控机制

创新设计"动态编排—并行推理—辩论-仲裁—双层校验—动态退火反思"的协同架构。辩论-仲裁模式让不同智能体就争议观点进行多轮辩论，由仲裁智能体依据证据链裁决，减少单一模型偏见；动态退火策略根据驳回原因自动分类并生成针对性修正提示词，同时衰减不通过智能体的发言权重（衰减因子 0.7，下限 0.2），确保生成资源的教学质量。实测退火修正成功率达 **92%**。

### 创新点三：对话式 8 维度动态画像构建

摒弃传统繁琐表单，通过自然语言对话自动抽取 8 个维度的学习特征，每个维度包含枚举化等级和结构化描述字段，支持画像随学随新，为个性化资源生成提供精准的学情基础。画像维度覆盖率达 **100%**。

### 创新点四：极速通道与深度推理双路径设计

通用知识问答通过 IntentNode 智能分流走极速通道，个性化学习需求自动升维至八智能体协同推理模式，完美兼顾响应效率与个性化深度。难度评分驱动智能体动态编排，按需分配算力。

### 创新点五：学习激励与情感陪伴机制

学习激励智能体根据学生情绪状态维度，在学习过程中适时插入鼓励、阶段性反馈或调整学习节奏的建议，实现从"知识传递"到"情感陪伴"的教育升级。

### 创新点六：画像—资源—辅导—路径—评估的五维闭环

画像构建提供个性化基础，资源生成基于画像产出定制内容，辅导模块提供实时答疑，路径模块规划学习顺序，评估模块量化效果并驱动画像更新，完成了从"单次问答工具"到"持续个性化学习平台"的转变。资源推送关联率达 **92%**，优化建议针对率 **92%**。

### 创新点七：本地知识库与 PubMed 国际文献的双重循证机制

创新设计"本地权威指南 + 国际前沿文献"的双重证据保障体系。本地 ChromaDB 知识库提供权威指南的精准溯源，PubMed E-utilities API 提供最新国际研究进展（内置 8 级证据等级排序），两者互补实现证据来源的双重覆盖。

### 创新点八：面向医学生的代码辅助开发能力

创新性地将代码辅助开发融入医学教育场景，支持医学数据分析编程、临床决策支持代码生成与医学 AI 模型实操，实现"理论学习 + 编程实操"的融合训练。

### 创新点九：全配置化与高可扩展的工程架构

提示词模板、专家角色、报告模板、校验规则、参数限制完全实现配置化与解耦管理。辩论轮数、退火衰减因子、难度阈值、意图-专家映射等关键参数均可通过配置文件灵活调整，为跨学科的低成本系统迁移打下坚实基础。

### 创新点十：自愈式退火反思与量化质量保障体系

系统创新性地实现了"驳回→分类→衰减→修正→重试"的自愈式反思循环，配合 71 条自动化测试用例（含白盒路径覆盖 100%）、三级并发压测、16 条安全测试与 7 条容灾测试，构建了完整的量化质量保障体系，确保系统在真实生产环境下的可靠性与稳定性。

---

## 八、应用价值与落地前景

| 应用方向 | 说明 |
|:---|:---|
| **高校个性化学习平台** | 直接作为高校智能学习平台的核心引擎，覆盖学习全生命周期，显著提升学习效率与教学质量 |
| **教师教学辅助工具** | 帮助教师快速生成适配不同学生水平的教学资源，大幅减轻备课负担 |
| **医学教育专项应用** | 以脑卒中医学教育为示范领域，提供从理论学习到临床思维训练的全链路支持，多模态联合推理支持医学影像分析，PubMed 提供前沿循证医学证据，代码辅助培养信息素养与编程能力 |
| **跨学科推广前景** | 得益于模块化架构与全配置化设计，更换知识库文档与专家配置即可迁移至心血管、肿瘤等其他医学教育领域，甚至拓展至工程、法律等教育场景 |
| **技术架构的示范价值** | 三层解耦架构与全链路 SSE 流式通信机制，为构建真实的智能教育生产系统提供了极具参考价值的技术范本 |

---

## 九、赛题要求对应性说明

### （一）对话式学习画像自主构建

| 赛题要求 | 系统实现 |
|:---|:---|
| 支持通过自然语言对话自动抽取特征 | 画像对话智能体引导学生自然对话，特征抽取智能体自动从对话中提取结构化特征 |
| 包含不少于 6 个维度的动态学生画像 | 实现 8 个维度：知识基础、认知风格、学习目标、易错模式、学习节奏、资源偏好、临床经验、情绪状态 |
| 支持画像的随学随新 | 对话结束后自动提取画像维度，支持手动编辑修正，版本化存储 |

### （二）多智能体协同的资源生成

| 赛题要求 | 系统实现 |
|:---|:---|
| 体现"多智能体"架构设计 | 8 个专业智能体协同工作，基于 LangGraph StateGraph 编排，支持辩论-仲裁与动态退火反思 |
| 个性化资源生成 | 根据学生画像的知识水平与认知风格，自动调整资源难度与内容深度 |
| 多类型资源生成 | 支持 7 类资源：课程讲解文档、思维导图、练习题目、临床指南、视频脚本、医学编程实操、代码实操案例 |
| 学习激励与情感陪伴 | 学习激励智能体根据情绪状态提供鼓励与节奏调整建议 |
| 智能体动态编排 | 根据意图类型和难度评分动态裁剪参与智能体数量（3-6 位 + 仲裁） |

### （三）学习路径规划与动态调整

| 赛题要求 | 系统实现 |
|:---|:---|
| 个性化学习路径规划 | 基于学生画像与学习目标，自动生成 5-15 步学习路径，含前置依赖 |
| 动态调整能力 | 支持插入步骤、更新资源、调整难度三种调整类型 |
| 进度追踪 | 支持标记完成状态、记录投入时长与自评反馈 |
| 精准资源推送 | 基于画像维度综合计算（0.3×关键词 + 0.3×难度 + 0.2×偏好 + 0.2×薄弱项），推送匹配的学习资源 |

### （四）学习效果评估

| 赛题要求 | 系统实现 |
|:---|:---|
| 学习效果评估 | 5 维度量化评估：知识掌握度、学习效率、技能应用、学习一致性、进度对齐度 |
| 知识点掌握热力图 | 按知识点树状结构展示掌握程度（0.0-1.0 四档） |
| 评估报告生成 | 基于多智能体推理生成结构化评估报告，含薄弱环节定位与改进建议 |
| 闭环反馈 | 评估结果自动触发学习路径调整和画像更新 |

### （五）技术架构要求

| 赛题要求 | 系统实现 |
|:---|:---|
| 大模型应用 | 基于 Qwen-Max/Plus/Turbo 三级大模型 + qwen-vl-max 多模态模型 |
| 多智能体协同 | 基于 LangGraph StateGraph 编排 8 个智能体，支持辩论-仲裁、动态退火反思与动态编排 |
| RAG 检索增强 | ChromaDB + BM25 混合检索 + 三阶漏斗（RRF 粗排 + Reranker 精排，4 模型容灾切换）+ QA 自动衍生引擎 + 检索缓存 |
| 流式交互 | 全链路 SSE 流式通信，前端实时打字机渲染 + 思考过程展示 + 断线续传 |
| 多模态能力 | qwen-vl-max 多模态视觉大模型，支持图片类型自动识别、流式影像分析、多图联合分析 |
| 国际文献检索 | PubMed E-utilities API，8 级证据等级排序，与本地 RAG 互补实现双重循证保障 |
| 代码辅助开发 | 代码实操案例生成、代码执行沙箱、代码补全与错误诊断 |
| 安全与质量保障 | JWT 双向认证 + 分布式限流 + 71 条自动化测试（白盒路径覆盖 100%）+ 16 条安全测试 + 7 条容灾测试 |